getenv(): Se obtiene la variable de entorno DOCSERVER_PORT
getcwd(): Se obtiene el directorio de trabajo
socket(): Se crea el socket
htonl(): Se convierte la dirección IP en formato de red, Big Endian
htons(): Se convierte el número del puerto en formato de red, Big Endian
bind(): Se asocia una dirección de red al socket
close(): se cierra el archivo con descriptor de archivo "3" 
listen(): Se pone el socket en modo escucha
htonl(): Se convierte la dirección IP en formato de red, Big Endian
htons(): Se convierte el número del puerto en formato de red, Big Endian
accept(): Se acepta la conexión en el socket
close(): se cierra el archivo con descriptor de archivo "4" 
recv(): Leer datos entrantes en el socket
inet_ntop(): se convierte la direccion IP a texto
pipe(): se crea una tubería
fork(): se crea un proceso hijo
close(): se cierra el extremo de escritura de la tubería
fork(): se crea un proceso hijo
close(): se cierra el extremo de lectura de la tubería
dup2(): se duplica el descriptor de archivo
setenv(): se setea la variable de entorno REQUEST_PATH
setenv(): se setea la variable de entorno SERVER_BASEDIR
setenv(): se setea la variable de entorno REMOTE_PORT
setenv(): se setea la variable de entorno REMOTE_IP
waitpid(): se espera a la finalización del proceso hijo
read(): se leen los datos del extremo de lectura de la tubería con el tamaño máximo de buffer
append(): se añaden los datos al array de lectura
read(): se leen los datos del extremo de lectura de la tubería con el tamaño máximo de buffer
append(): se añaden los datos al array de lectura
read(): se leen los datos del extremo de lectura de la tubería con el tamaño máximo de buffer
append(): se añaden los datos al array de lectura
read(): se leen los datos del extremo de lectura de la tubería con el tamaño máximo de buffer
append(): se añaden los datos al array de lectura
read(): se leen los datos del extremo de lectura de la tubería con el tamaño máximo de buffer
append(): se añaden los datos al array de lectura
read(): se leen los datos del extremo de lectura de la tubería con el tamaño máximo de buffer
append(): se añaden los datos al array de lectura
read(): se leen los datos del extremo de lectura de la tubería con el tamaño máximo de buffer
append(): se añaden los datos al array de lectura
read(): se leen los datos del extremo de lectura de la tubería con el tamaño máximo de buffer
append(): se añaden los datos al array de lectura
close(): se cierra el extremo de lectura de la tubería
send(): Se envían los datos de la cabecera a través del socket
send(): Se envían los datos del cuerpo a través del socket
htonl(): Se convierte la dirección IP en formato de red, Big Endian
htons(): Se convierte el número del puerto en formato de red, Big Endian
